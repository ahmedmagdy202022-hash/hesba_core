# Layout Maps with Spaces

Use `component_region_map.json` to understand the regions and safe areas.
Do not position elements by guessing from screenshots.
Implementation should use CSS grid/flex/clamp and the contracts in `05_CONTRACTS`.
