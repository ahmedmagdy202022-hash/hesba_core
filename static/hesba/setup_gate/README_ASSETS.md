# Setup Gate Component Assets V2

This package replaces the previous component assets package.

Important:
- Use these assets only.
- Do not use the previous package if any step icon crop includes Arabic text/card pieces.
- Step icon files in `assets/setup_gate_step_*_icon.png` are clean 128x128 PNGs.
- Optional glyph-only versions are available as `assets/setup_gate_step_*_icon_glyph.png`.
- Activity icons are the approved cropped assets from the approved visual reference.
- Do not redraw or replace icons with FontAwesome/generic icons.
- Do not open PR or merge without Main Control Chat approval.
